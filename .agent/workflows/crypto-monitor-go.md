---
description: 크립토모니터(CryptoMonitor) Go 프로젝트 표준 개발 워크플로우 (v1.0.1)
---

# 🚀 크립토모니터 Go 개발 워크플로우 (Workflow)

이 문서는 고성능 비동기 아키텍처와 강화된 보안 수칙을 바탕으로 일관된 품질을 유지하며 프로젝트를 확장하기 위한 표준 가이드를 정의합니다.

---

## 💎 핵심 개발 및 설계 원칙

1. **비동기 스트림 지향 (Async Stream Oriented)**
   - 모든 시세 수집은 워커에서 비동기 채널(`TickerChan`)을 통해 인터럽트 없이 전달되어야 한다.
2. **소수점 정밀도 보장 (Decimal Precision)**
   - 인프라 레이어 수집 즉시 `decimal.Decimal`로 변환하며, 모든 산식(Premium, Gap)은 부동소수점 오차 없이 처리한다.
3. **복원력 있는 인프라 (Resilient Infra)**
   - 모든 외부 연결(WS/HTTP)은 지수 백오프 기반 재연결 및 패닉 복구 로직이 필수다.
4. **보안 및 봇 회피 (Security & Bot Avoidance)**
   - 모든 요청에는 브라우저 환경을 위장한 `User-Agent`를 포함하며, 거래소별 권장 심볼 수 제한을 준수한다.

---

## 🛠️ 환경 설정 및 실행

```bash
# 1. 의존성 설치
go mod tidy

# 2. 로컬 설정 확인 (configs/config.yaml)

# 3. 빌드 및 콘솔 실행 확인
go build -o cryptomonitor ./cmd/app
./cryptomonitor
```

---

## ✅ 필수 개발 절차

### 1단계: 개발 전 (Preparation)
1. **기술 명세 확인**: [docs/SPECIFICATIONS.md](../docs/SPECIFICATIONS.md)에서 현재 도메인 모델과 API 스펙을 확인한다.
2. **아키텍처 검토**: [docs/ARCHITECTURE.md](../docs/ARCHITECTURE.md)를 통해 비동기 흐름과 레이어 규칙을 숙지한다.

### 2단계: 코딩 중 (Coding)
1. **인터페이스 구현**: 새로운 거래소 추가 시 `ExchangeWorker` 인터페이스를 구현하고 `PriceService.GetTickerChan()`에 연결한다.
2. **심볼 검증**: `subscribe` 메서드 구현 시 50개 등 거래소별 심볼 제한 로직을 반드시 포함한다.

### 3단계: 검증 및 문서화 (Verification & Docs) 🚀
// turbo-all
1. `go test ./...` 및 `go test -race` 수행 (동시성 안전 점검)
2. `go build ./...` (전체 빌드 확인)
3. **문서 동기화**: 프로젝트 변경 시 아래 문서들을 반드시 최신화한다.
   - [README.md](../README.md): 마일스톤 및 주요 기능 갱신
   - [docs/SPECIFICATIONS.md](../docs/SPECIFICATIONS.md): API 스펙 및 운영 정책 갱신

---

## 📝 변경 이력 및 마일스톤

- **v1.0.1 (2026-01-03)**: 비동기 시세 파이프라인(Channel) 전환, 문서 통합(SSOT) 및 보안 고도화 완료.
- **v1.0.0**: 초기 릴리스 및 웹소켓 연동 기반 구축.

---
*Last Updated: 2026-01-03*
